#include <iostream>
#include <cmath>

using namespace std;

int main() {
    int radius;
    float area;

    cout<<"Enter radius of the circle: ";
    cin >> radius;
    area =(M_PI)*(radius)*(radius);
    cout << "The area of the circle is: " << area;

}